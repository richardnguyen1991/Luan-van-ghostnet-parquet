project:
  mode: practical_baseline

preprocessing:
  missing_strategy: median

feature_extraction:
  sax_enabled: false
  resnet50_enabled: false

feature_selection:
  cfaco_enabled: false

graph:
  enabled: false
  status: pending_step_3_schema_group_validation

